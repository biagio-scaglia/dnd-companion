import 'package:flutter/material.dart';

class AppRadius {
  static const double xs = 4.0;
  static const double s = 8.0;
  static const double m = 12.0;    // Bottoni, input
  static const double l = 16.0;    // Card standard
  static const double xl = 20.0;   // Card featured
  static const double pill = 32.0; // Chip, badge

  static final BorderRadius xsBorderRadius = BorderRadius.circular(xs);
  static final BorderRadius sBorderRadius = BorderRadius.circular(s);
  static final BorderRadius mBorderRadius = BorderRadius.circular(m);
  static final BorderRadius lBorderRadius = BorderRadius.circular(l);
  static final BorderRadius xlBorderRadius = BorderRadius.circular(xl);
  static final BorderRadius pillBorderRadius = BorderRadius.circular(pill);
}

