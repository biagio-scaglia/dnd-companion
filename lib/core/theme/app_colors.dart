import 'package:flutter/material.dart';

class AppColors {
  // Background & Surfaces (Base Calda Marrone)
  static const Color background = Color(0xFF1C110A);
  static const Color surface = Color(0xFF2C1A11);
  static const Color surfaceSecondary = Color(0xFF3D261C);

  // Text (Parchment White)
  static const Color textPrimary = Color(0xFFF4F1E8);
  static const Color textSecondary = Color(0xFFB7B2A5);

  // Accents (Giallo e Oro Luminosi)
  static const Color magicAccent = Color(0xFFFFD54F); // Giallo ambra acceso
  static const Color naturalAccent = Color(0xFF00E676); // Verde smeraldo
  
  // CTA & Highlights
  static const Color highlight = Color(0xFFFFB300); // Oro caldo
  
  // States
  static const Color danger = Color(0xFFFF5252); // Rosso vivo
}
